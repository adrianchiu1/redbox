Drop hand-downloaded files here as `{SOURCE_ID}/{part}.{ext}` (see DOWNLOAD_LIST.md),
then run `ggfiscal debt ingest-incoming`. Files here are not committed; the
snapshots the command creates are (D-S7-001).
