# HANDOFF.md

Rewritten 2026-09-03, end of session 4 (Stage 6 completed on
`claude/stage-6-gg-fiscal-51xbzu`; Stages 0–5 were merged to `main` at
66dea1e).

## Current stage

**Stage 6 (packaging and vintage re-run) — COMPLETE. Gate 6 PASSED.
All §12 stages done; the pipeline is in maintenance mode.**

## Gate 6 status: PASSED

| Gate 6 requirement | Status |
|---|---|
| Any stitched value reproducible from anchor and recorded growth using only deliverables | **Done.** `tests/stage_6/test_gate6.py` reconstructs every stitched/forecast observation (>900 across both trees, both variants) from the published CSVs alone: value = neighbouring year's value × the row's recorded growth_rate, chained to the anchor row, verified to 1e-9; every derived row names its growth source, rate and anchor (D-S6-005). |
| All §1 objectives met | **Done.** 66 line series in both variants + three ledgers (identities tested); reconciliation tables for every registered WEO vintage and both variants; declared endpoints per D7 with recorded reasons; nothing scaled to WEO (D13 — tested §8.6). |
| §11.6 deliverables | **Complete.** Generated README.md (deliverable 10, replaces the stub — regenerated by `ggfiscal report`, hand-edits do not survive); `reports/validation_report.html`; `data/canonical/crosswalks.csv` (§11.5 concatenation); run-manifest completeness (D-S6-003: input hashes at build + per-deliverable sha256/rows updated by every pipeline step). |
| §11.7 vintage re-run | **Done.** `ggfiscal detect-vintages` (metadata tier + `--hash` content tier) writes `reports/vintage_diff.md`; the WEO vintage register moved into `config/sources.yaml` (`IMF_WEO.api.vintages`, D-S6-001) so a new edition is ONE config entry + rebuild, no code change — demonstrated with a simulated 2026-October edition fixture (`tests/stage_6/test_vintages.py`). Live run 2026-09-03: no new edition; register metadata refreshed where live values had drifted (D-S6-002). |

`python3 -m pytest`: **90 passed** (71 prior + 19 stage_6).
`ggfiscal validate`: **OK=55 WARN=661, no ERROR, no SKIP** — all 28
V-checks run. WARN delta vs Stage 5's recorded 582 is by-design tiers only:
S0_SNAPSHOTS grew because the append-only manifest now carries three
sessions of pulls whose earlier raw bytes are not in this fresh container
(D-S0-004), plus data-driven counts on the fresher 2026-09-03 harvest.
No canonical value changed by hand anywhere (D13).

## What Stage 6 added

`src/ggfiscal/vintages.py` (§11.7 detection: IMF catalog diff vs the config
vintage register, GFS version watch, Eurostat UPDATE_DATA annotations, ONS
releaseDate, optional full content-hash tier; blocked/PDF sources reported
with OQ refs). `src/ggfiscal/manifest.py` (run-manifest completeness).
`src/ggfiscal/report/validation.py` (validation_report.html) and
`report/readme.py` (generated README). `build.write_crosswalks()`
(crosswalks.csv). `endpoints.weo_vintages()` now reads config
(D-S6-001; the old WEO_VINTAGES constant is gone — all consumers updated).
CLI: `detect-vintages` real (`--hash` optional); `report` writes the
validation report, regenerates source_register.csv and README, and
completes the manifest; `reconcile`/`validate`/`detect-vintages` refresh
the manifest's deliverable hashes. Tests: `tests/stage_6/` (8 vintage tests
incl. the simulated-edition fixture; 11 Gate 6 tests).

## Blocked on whom

Nothing blocks the pipeline; these limit its reach (all committee items):
- **OQ-6:** obr.uk (Cloudflare) / gov.uk / bmas.de (egress) blocked — every
  GBR-specific forecast source. Unblocking OBR would transform the GBR
  explained share (now ~0–6%).
- **OQ-7:** AMECO→DSM long-term interest join withheld (V16/D12);
  FRA/DEU GF01_7 strict ends 2027 pending the committee's call —
  config-only to apply.
- **OQ-5:** pre-1995 archives need §11.4 second keying or machine-readable
  archive endpoints.

## Exact next command

Maintenance cadence (no further build stages exist):

```
ggfiscal detect-vintages        # diff live metadata vs the register
# on a new WEO edition: add one entry to config/sources.yaml
#   (IMF_WEO.api.vintages, latest first), then:
ggfiscal fetch --all && ggfiscal build && ggfiscal reconcile && ggfiscal report && python3 -m pytest && ggfiscal validate
```

Fresh container first (raw snapshots are not committed, D-S0-004):
`pip install -e .[dev]` (plus xlrd/openpyxl/pytest into the same
interpreter; use `python3 -m pytest`), then `ggfiscal fetch --all` before
any build. Expected green baseline: 90 passed; validate no ERROR, no SKIP
(WARN counts drift with live data and manifest history — ERROR/SKIP are the
invariants).

## Data facts future sessions must not rediscover

- All Stage 3/4/5 facts (HANDOFF history at 80f877f, f32d7a7, 66dea1e) and
  D-S0-001..D-S6-005.
- WEO vintages are registered in `config/sources.yaml`
  (`IMF_WEO.api.vintages`), latest first; `endpoints.weo_vintages()` is the
  only reader. The IMF API exposes 3 editions today and DROPS old ones —
  snapshot new editions promptly (D-S0-007).
- The IMF catalog JSON embeds a per-request timestamp: it is excluded from
  the `--hash` tier by design (D-S6-002); its diff is semantic.
- Eurostat live-metadata check: SDMX 2.1 dataflow `?detail=full`,
  annotations UPDATE_DATA / OBS_PERIOD_OVERALL_LATEST (namespace prefix
  varies — parse by local name). ONS: dataset landing `/data` JSON,
  `description.releaseDate` (date part only).
- Register verification fields must match live-observed values or
  detect-vintages reports `changed` — that is the intended signal to
  refresh sources.yaml (a config change), re-fetch and rebuild.
- run manifests: `build` writes the base (inputs); later steps only update
  `deliverables`. The latest run_*.json is authoritative; older ones are
  history. Canonical CSV rows embed `run_id`, so every rebuild diffs every
  committed canonical CSV — expected, not a data change.
- pandas 3.x works (this session ran 3.0.5); no code change was needed.
- README.md is generated (§11.6 deliverable 10) — never hand-edit it;
  narrative lives in HANDOFF/DECISIONS/OPEN_QUESTIONS.

## §15 dependencies currently riding on defaults

Unchanged from Stage 5: Q1, Q3 (grow_with_proxy), Q4 (D-S2-002 V5;
D-S3-005 V16; D-S5-002 GBR sigma 0.5), Q7 (moot, OQ-6), Q8, Q10
(exercised: residuals published as lumps), Q13 defaults; Q11 pinned
(D-S0-007, register now in config per D-S6-001); Q12 deviated by necessity
(AMECO independent total for GBR — D-S5-002; revisit when OBR is
reachable).
